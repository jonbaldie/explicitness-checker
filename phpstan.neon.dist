# The coding standard (CODING_STANDARDS.md): production PHP passes level max.
# Run with `vendor/bin/phpstan analyse`. Add src/ to paths once it exists.
parameters:
	level: max
	paths:
		- bin/explicitness-checker
